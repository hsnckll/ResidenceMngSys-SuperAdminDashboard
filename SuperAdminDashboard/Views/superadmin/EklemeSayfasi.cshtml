@{
    Layout = "_SuperAdminLayout";
    ViewData["Title"] = "Yeni Apartman & Yönetici Ekle";
    ViewData["ActivePage"] = "Apartments";
}

<!-- Topbar -->
<header class="topbar" style="margin-bottom: 1rem;">
    <div>
       
        <h1 class="mb-1" style="font-size: 1.5rem;">Yeni Apartman & Yönetici Ekle</h1>
        <p style="font-size: 0.9rem;">Sisteme yeni bir apartman (müşteri) tanımlayın ve yöneticisini atayın.</p>
    </div>
</header>

@if (TempData["Error"] != null)
{
    <div class="alert-error">
        <i class="fa-solid fa-circle-exclamation"></i> @TempData["Error"]
    </div>
}
<form asp-action="ApartmanveYoneticiEkleme" asp-controller="SuperAdmin" method="post" id="apartmanForm">
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(450px, 1fr)); gap: 1.5rem; align-items: stretch;">
        <!-- Sol Sütun: Apartman Bilgileri -->
        <div class="glass-panel" style="padding: 1.75rem; display: flex; flex-direction: column;">
            <div style="display: flex; align-items: center; gap: 1rem; border-bottom: 1px solid var(--surface-border); padding-bottom: 1rem; margin-bottom: 1.25rem;">
                <div style="width: 44px; height: 44px; border-radius: var(--radius-md); background: rgba(59, 130, 246, 0.1); color: var(--primary-color); display: flex; align-items: center; justify-content: center; font-size: 1.15rem;">
                    <i class="fa-solid fa-building"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.15rem;">Apartman Bilgileri</h3>
                    <p style="font-size: 0.85rem; margin-top: 0.2rem;">Sisteme kaydedilecek apartmanın temel detayları</p>
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">Apartman / Site Adı</label>
                <input type="text" name="ApartmentName" class="form-control" placeholder="Örn: Güneş Sitesi" required>
            </div>

            <div class="form-group">
                <label class="form-label">İl / İlçe</label>
                <input type="text" name="CityDistrict" class="form-control" placeholder="Örn: Kadıköy, İstanbul" required>
            </div>

            <div class="form-group" style="margin-bottom: 0; flex: 1; display: flex; flex-direction: column;">
                <label class="form-label">Açık Adres</label>
                <textarea name="Address" class="form-control" placeholder="Apartmanın tam açık adresi..." style="resize: none; flex: 1; min-height: 80px;"></textarea>
            </div>
        </div>

        <!-- Sağ Sütun: Yönetici Hesabı Bilgileri -->
        <div class="glass-panel" style="padding: 1.75rem; display: flex; flex-direction: column;">
            <div style="display: flex; align-items: center; gap: 1rem; border-bottom: 1px solid var(--surface-border); padding-bottom: 1rem; margin-bottom: 1.25rem;">
                <div style="width: 44px; height: 44px; border-radius: var(--radius-md); background: rgba(16, 185, 129, 0.1); color: var(--success); display: flex; align-items: center; justify-content: center; font-size: 1.15rem;">
                    <i class="fa-solid fa-user-shield"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.15rem;">Yönetici Hesabı</h3>
                    <p style="font-size: 0.85rem; margin-top: 0.2rem;">Sisteme giriş yapacak apartman yöneticisi bilgileri</p>
                </div>
            </div>

            <div style="flex: 1; display: flex; flex-direction: column; justify-content: space-between; gap: 1rem;">
                <div class="form-row" style="margin-bottom: 0;">
                    <div class="form-group" style="margin-bottom: 0;">
                        <label class="form-label">Yönetici Adı</label>
                        <input type="text" name="Name" class="form-control" placeholder="Örn: Ahmet" required>
                    </div>
                    <div class="form-group" style="margin-bottom: 0;">
                        <label class="form-label">Yönetici Soyadı</label>
                        <input type="text" name="Surname" class="form-control" placeholder="Örn: Yılmaz" required>
                    </div>
                </div>

                <div class="form-group" style="margin-bottom: 0;">
                    <label class="form-label">Yönetici E-posta (Kullanıcı Adı)</label>
                    <input type="email" name="Mail" class="form-control" placeholder="yonetici@apartman.com" required>
                </div>

                <div class="form-group" style="margin-bottom: 0;">
                    <label class="form-label">Telefon Numarası</label>
                    <input type="tel" name="PhoneNumber" class="form-control" placeholder="Örn: 05XX XXX XX XX" required>
                </div>

                <div class="form-row" style="margin-bottom: 0;">
                    <div class="form-group" style="margin-bottom: 0;">
                        <label class="form-label">Geçici Şifre</label>
                        <input type="password" name="Password" class="form-control" placeholder="••••••••" required>
                    </div>
                    <div class="form-group" style="margin-bottom: 0;">
                        <label class="form-label">Geçici Şifre (Tekrar)</label>
                        <input type="password" name="ConfirmPassword" class="form-control" placeholder="••••••••" required>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Aksiyon / Submit Alanı -->
    <div class="glass-panel" style="margin-top: 1.5rem; padding: 1.25rem 1.75rem; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
        <div style="display: flex; align-items: center; gap: 0.75rem;">
            <i class="fa-solid fa-circle-info text-muted"></i>
            <p style="margin: 0; font-size: 0.9rem;">Lütfen girdiğiniz bilgilerin doğruluğunu kontrol ediniz.</p>
        </div>
        <div style="display: flex; gap: 1rem;">
            <a href="/SuperAdmin/Dashboard" class="btn btn-outline" style="min-width: 120px;">İptal</a>
            <button type="submit" class="btn btn-primary" style="padding: 0.75rem 2rem; box-shadow: 0 4px 14px 0 rgba(59, 130, 246, 0.39);">
                <i class="fa-solid fa-check" style="margin-right: 0.5rem;"></i> Kaydet ve Oluştur
            </button>
        </div>
    </div>
</form>

<script>
    document.getElementById('apartmanForm').addEventListener('submit', function(e) {
        var password = document.querySelector('[name="Password"]').value;
        var confirm = document.querySelector('[name="ConfirmPassword"]').value;
        if (password !== confirm) {
            e.preventDefault();
            alert('Şifreler eşleşmiyor!');
        }
    });
</script>